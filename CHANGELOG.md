# 更新记录

## [Unreleased]

尚无变更。

## [0.1.0-beta.4] - 2026-08-27

### 发布与复盘

1. 内容概览始终显示四个平台的发布状态，未发布内容也能直接核对 `0/4` 分发进度。
2. 任一启用平台确认发布后，主流程立即进入「已发布」并开放复盘；其他平台继续作为分发进度显示。
3. 发布日期支持按真实日期修改，不再只能保留首次确认当天。
4. 复盘页支持人工填写、保存草稿和确认沉淀，没有 AI 会话也能完成完整流程。
5. AI 发布前诊断改为可选项，只有用户明确采纳时才会改写下一步。

### 界面

1. 运营侧边栏改用与对话、内容一致的纸张色系，同时保留选中项的黑线白框。

## [0.1.0-beta.3] - 2026-08-27

### 品牌迁移

1. 插件包名、Bundle ID、设置命名空间和发布文件统一改为 `jacky-creator`。
2. GitHub Release 安装包改为 `jacky-creator-0.1.0-beta.3.tgz`。
3. 旧版工作台状态会安全复制到 `~/.jacky-creator/`；迁移不删除旧目录，也不覆盖已经存在的新数据。

### 文档

1. 精简 README 和安装说明，移除测试阶段说明与个人化示例。
2. 更新主视觉，移除 IP 后方圆环和“灵感”旁边的装饰嫩芽。
3. 安装包只携带用户需要的安装、使用和文件说明，不再包含内部开发文档。

## [0.1.0-beta.2] - 2026-08-26

这次更新补齐桌面工作流细节，并准备使用同一份 GitHub Release 安装包提交社区插件市场。

### 改进

1. **文件夹打开方式**：内容文件夹改用 DSH Host 原生接口打开，避免被侧栏文件编辑器当成普通文件拦截。
2. **演示画幅指令**：界面显示 `16:9` 和 `3:4`，输出文件名仍使用稳定的 `16x9` 和 `3x4` 标识。
3. **导出提示**：等待成片时直接显示目标内容目录，并说明 MP4 或 MOV 应放在目录根部。
4. **品牌与归属**：统一 Jacky Creator 对外文案，同时在 README 保留上游项目与原 Creator Cockpit 的来源说明。

### 发布

1. 官方 DSH 运行时包改为 Peer 依赖，避免插件安装时重复携带宿主组件。
2. README、安装文档、更新命令和市场说明统一指向 `v0.1.0-beta.2` 的预构建包。

## [0.1.0-beta.1] - 2026-08-25

首个 Jacky Creator GitHub 测试版。暂不发布 npm，也不提交插件市场。

### 新增

1. **内容资产流**：内容详情按概览、脚本、演示、视频、字幕、封面和文章分开处理，进度统一为「选题 → 脚本 → 演示 → 视频 → 字幕 → 封面 → 文章 → 发布」。
2. **工作台内编辑**：概览聚焦选题、一句话核心、标签和运营策略，一句话核心可直接编辑并写回真实 `topic.md`。
3. **灵活制作路线**：演示、字幕和文章可以跳过；Screen Studio 保留为可选项，也可以使用剪映或其他工具并等待成片落盘。
4. **封面工作区**：封面资产支持 3:4、4:3、16:9 三种画幅，在工作台内切换、放大预览并查看生成状态。
5. **品牌界面**：内容与运营区域统一使用纸张色、克兰因蓝、黑色硬边和实体投影，补齐窄面板下的横向滚动与布局适配。

### 修复

1. 修复封面点击「显示」后图片裂开的问题。
2. 修复概览下一步没有进入对应资产页的问题。
3. 修复重复等待成片时旧监听器可能清除新监听器的问题。
4. 防止已删除内容写入无效的跳过状态。
5. 修复 npm 10 在打包 JSON 前输出构建日志时，发布门禁误判为不可解析的问题。
6. 移除 Client 构建产物中的构建机绝对路径，并增加发布前隐私回归检查。

### 发布与文档

1. 公开仓库、Issue 和 README 迁移到 `Jackywxsz/DSH-Creator`。
2. DeepSeek Harness 开发与兼容基线升级到 `0.1.1-rc.2`，与 DSH Desktop 2.0.2 对齐。
3. 增加小白安装、首次配置、升级、卸载和故障排查说明。
4. 固化 GitHub Beta → npm 稳定包 → dsh-market 的发行路径。
5. 增加 CI、Security Policy、贡献说明和 Jacky 品牌资产边界。
